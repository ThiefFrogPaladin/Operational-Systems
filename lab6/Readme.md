# Отчет по лабораторной работе
Состав бригады: Васильев 
Номер варианта: 3
Описание поставленной задачи
1. Для сдачи лабораторной работы необходимо:
1.1. Программно реализовать алгоритм согласно варианту бригады 3 с любой рекомендуемой структурой данных: массив
1.2. Измерить время работы алгоритма на различных наборах данных увеличивающегося размера (например, на массивах размером 100, 1000, 10000, 100000 и т.д.). Данные можно генерировать с помощью генератора случайных чисел внутри самой программы.
1.3. Результаты измерения времени работы представить в отчете виде таблицы или графика. Отчет должен содержать состав бригады, номер варианта, краткое описание реализованного алгоритма, используемые структуры данных, разработанный исходный код.
В случае невозможности прямой реализации алгоритма над выбранной структурой данных, рекомендуется следующий подход: получение данных в выбранной структуре -> извлечение данных в массив -> реализация алгоритма над массивом -> заполнение выходной структуры данных.

## Краткое описание реализованного алгоритма
Алгоритм последовательно перебирает все элементы исходного массива и формирует новый массив, содержащий только нечетные числа. При этом четные числа отбрасываются. Временная сложность алгоритма составляет O(n), где n - размер входного массива, так как каждый элемент обрабатывается ровно один раз.

## Используемые структуры данных
В качестве основной структуры данных использовался ArrayList, реализованный в Python как встроенный тип list. Данная структура обеспечивает эффективный последовательный доступ к элементам и динамическое расширение при добавлении новых элементов.

## Результаты измерения времени работы
================================================================================
+------------------+-------------------+-------------------------+---------------+
|   Размер массива |   Исходный размер |   Размер после удаления |   Время (сек) |
+==================+===================+=========================+===============+
|              100 |               100 |                      40 |      1.2e-05  |
+------------------+-------------------+-------------------------+---------------+
|             1000 |              1000 |                     506 |      0.0001   |
+------------------+-------------------+-------------------------+---------------+
|            10000 |             10000 |                    5086 |      0.000827 |
+------------------+-------------------+-------------------------+---------------+
|           100000 |            100000 |                   50100 |      0.007214 |
+------------------+-------------------+-------------------------+---------------+
|           500000 |            500000 |                  250268 |      0.045369 |
+------------------+-------------------+-------------------------+---------------+
|          1000000 |           1000000 |                  500298 |      0.08405  |
+------------------+-------------------+-------------------------+---------------+
================================================================================

*Примечание: точные значения могут незначительно отличаться при каждом запуске программы из-за случайной генерации данных и особенностей работы операционной системы.*

## Выводы
В ходе лабораторной работы был реализован алгоритм удаления четных чисел из массива с использованием структуры данных ArrayList. Проведены измерения времени работы алгоритма на массивах различного размера. Полученные результаты подтверждают линейную зависимость времени работы от размера входных данных, что соответствует теоретической оценке O(n).

График зависимости времени от размера массива представлен на рисунке 1. На графике видно, что при увеличении размера массива в 10 раз время выполнения также увеличивается примерно в 10 раз, что подтверждает линейную сложность алгоритма.
<img width="1240" height="753" alt="image" src="https://github.com/user-attachments/assets/cf6eef2a-8898-445a-aa3a-4b0cd835c33d" />
